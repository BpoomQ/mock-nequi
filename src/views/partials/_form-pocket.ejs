
<div class="modal fade" tabindex="-1" role="dialog" id="pocketModal" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Nuevo bolsillo</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <form action="/addPocket" method="POST" name="Form">
          <div class="form-group">
            <input type="text" class="form-control" name="pocketName" placeholder="Nombre del bolsillo" required>
            <input type="text" hidden name="id" value="<%= user._id %>">
          </div>
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Cerrar</button>
          <button type="submit" class="btn btn-primary">Crear</button>
        </form>
      </div>
    </div>
  </div>
</div>
